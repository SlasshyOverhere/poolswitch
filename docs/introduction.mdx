---
title: Introduction
description: "PoolSwitch lets developers use multiple API keys without writing failover and rotation logic by hand."
---

## Why PoolSwitch exists

Free-tier and quota-limited APIs are often too small for real applications.

If an API only gives you `100 searches per month`, one key is rarely enough. Most teams end up rebuilding the same plumbing over and over:

- rotate across several keys
- detect `429` or quota exhaustion
- put bad keys into cooldown
- retry safely with another account
- track which key is healthy right now

`PoolSwitch` packages that logic so developers can focus on their app instead of account juggling.

## What is PoolSwitch?

`poolswitch` is designed for provider APIs such as OpenAI, Anthropic, Groq, Google, HuggingFace, or any generic HTTP endpoint that authenticates requests with an API key header.

It can run in two modes:

1. **Embedded package mode**
   Use the Python client directly inside your app. This is the main product experience for Python users.
2. **Proxy mode**
   Run a local or shared HTTP proxy when you want language-agnostic access from Node.js, Go, curl, or multiple services.

## Key Features

<CardGroup cols={2}>
  <Card title="No Manual Failover Logic" icon="shield-check">
    Developers do not need to hand-roll round robin, retries, or cooldown logic in every project.
  </Card>
  <Card title="Embedded Package" icon="box-open">
    Use PoolSwitch directly inside a Python app with a normal client API.
  </Card>
  <Card title="Optional Proxy Server" icon="server">
    Run the same routing engine as a local or shared HTTP gateway when your stack is multi-language.
  </Card>
  <Card title="Built for Quota-Limited APIs" icon="rotate">
    Works well for APIs with monthly usage caps, free tiers, and per-account limits.
  </Card>
  <Card title="Flexible Routing" icon="route">
    Choose `round_robin`, `least_used`, `random`, or `quota_failover`.
  </Card>
  <Card title="Persistent State" icon="database">
    Keep key state in memory, Redis, or SQLite.
  </Card>
</CardGroup>

## Which mode should you use?

<CardGroup cols={2}>
  <Card title="Embedded Python Client" icon="bolt" href="/docs/embedded-python">
    Best when your application is already in Python and you want a direct in-process package.
  </Card>
  <Card title="Proxy Mode" icon="network-wired" href="/docs/proxy-mode">
    Best when multiple languages or services need to share the same key pool over HTTP.
  </Card>
</CardGroup>

## Start here

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/docs/quickstart">
    Get from install to your first rotating request in a few minutes.
  </Card>
  <Card title="Mintlify Deployment Guide" icon="globe" href="/docs/deploying-with-mintlify">
    Publish a polished docs site like OpenClaw using Mintlify.
  </Card>
</CardGroup>
