{
  "name": "poolswitch-docs",
  "private": true,
  "description": "Build and preview the PoolSwitch documentation site.",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs --host 127.0.0.1 --port 3000",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs --host 127.0.0.1 --port 4173"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
